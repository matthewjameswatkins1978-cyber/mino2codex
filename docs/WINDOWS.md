# Windows

Install Nushell and Codex normally, run `nu install.nu`, open a new Nushell session, and run `m2c setup`. State is under Nushell's platform-aware data directory, not in `%USERPROFILE%\\.codex` and not in the repository. The normal `codex` command is not modified.
